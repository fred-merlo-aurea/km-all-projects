@using KMEnums
@using KMModels.Controls.Standard.Common

<script>
    array[@((int)ControlType.Zip)] = { 
        name: '@ControlType.Zip', 
        object: @Html.Raw(Json.Encode(new Zip(true))),
        control: "<input type='text' class='input'/>"
    }
</script>