@using KMEnums
@using KMModels.Controls

<script>
    array[@((int)ControlType.TextArea)] = { 
        name: '@ControlType.TextArea', 
        object: @Html.Raw(Json.Encode(new TextArea(true))),
        control: "<textarea class='input'></textarea>" 
    }
</script>