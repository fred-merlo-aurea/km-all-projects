@using KMEnums
@using KMModels.Controls.Standard.Common

<script>
    array[@((int)ControlType.LastName)] = { 
        name: '@ControlType.LastName', 
        object: @Html.Raw(Json.Encode(new LastName(true))),
        control: "<input type='text' class='input'/>" 
    }
</script>