@using KMEnums
@using KMModels.Controls.Standard.Uncommon

<script>
    array[@((int)ControlType.Title)] = { 
        name: '@ControlType.Title', 
        object: @Html.Raw(Json.Encode(new Title(true))),
        control: "<input type='text' class='input'/>" 
    }
</script>