@using KMEnums
@using KMModels.Controls.Standard.Uncommon

<script>
    array[@((int)ControlType.FullName)] = { 
        name: '@ControlType.FullName', 
        object: @Html.Raw(Json.Encode(new FullName(true))),
        control: "<input type='text' class='input'/>" 
    }
</script>