@using KMEnums
@using KMModels.Controls

<script>
    array[@((int)ControlType.TextBox)] = { 
        name: '@ControlType.TextBox', 
        object: @Html.Raw(Json.Encode(new TextBox(true))), 
        control: "<input type='text' class='input'/>" 
    }
</script>