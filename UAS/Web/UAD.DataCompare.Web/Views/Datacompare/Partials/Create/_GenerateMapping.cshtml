
@model UAD.DataCompare.Web.Models.ColumnMapper

@Html.EditorForModel("ColumnMapper")


    @*@model UAD.DataCompare.Web.Models.FileViewModel

@{
    int counter = 1;
}
@if (Model.ColumnMapping != null)
{
    for (int i = 0; i < Model.ColumnMapping.Count; i++)
    {
        @Html.HiddenFor(modelItem => Model.ColumnMapping[i].myFieldMappingID)
        @Html.HiddenFor(modelItem => Model.ColumnMapping[i].myPreviewData)
        @Html.HiddenFor(modelItem => Model.ColumnMapping[i].mySourceColumn)
        @Html.HiddenFor(modelItem => Model.ColumnMapping[i].myUadColumn)
        <div class="row" style="border:1px solid gray; margin:5px 10px;">
            <div class="col-md-3 col-md-offset-2" style="padding:5px 0px;">
                @Html.TextBoxFor(modelItem => Model.ColumnMapping[i].mySourceColumn)
            </div>
            <div class="col-md-3" style="padding:5px 0px;">
                @Html.DropDownList("myUadColumn"+ @i, Model.ColumnMapping[i].fileColumnMapping, )
                <select name='dropDownMapper_@counter' required>
                    @foreach (UAD.DataCompare.Web.Models.CustomDropDown cd in Model.ColumnMapping[i]._dropDownMapper)
                    {
                        if (cd.SelectedItem != "Ignore")
                        {
                            <option selected>@cd.Item</option>
                        }
                        else
                        {
                            <option>@cd.Item</option>
                        }
                    }
                </select>

            </div>
            <div class="col-md-2 col-md-offset-2" style="padding:5px 0px;">
                <button value="Delete" class="btnDelete icon-kmcustom-delete" id="@Model.ColumnMapping[i].myFieldMappingID"></button>
            </div>
            <div class="col-md-8 col-md-offset-2" style="padding:5px 0px;">
                @Html.TextAreaFor(modelItem => Model.ColumnMapping[i].myPreviewData, new { @style = "width:88%;background-color:#dadada;", @readonly= true } )
            </div>
        </div>
       
        
    }

}*@

