CREATE TABLE [dbo].[ECNCampaign] (
    [ECNCampaignId]   INT           NOT NULL,
    [ECNCampaignName] VARCHAR (100) NULL,
    CONSTRAINT [PK_ECNCampaign] PRIMARY KEY CLUSTERED ([ECNCampaignId] ASC)
);
GO
