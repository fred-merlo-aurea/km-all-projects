CREATE PROCEDURE [dbo].[ccp_VCast_BeforeDQM]
AS
BEGIN
	-- SET NOCOUNT ON added to prevent extra result sets from interfering with SELECT statements.
	SET NOCOUNT ON;

	-- Set DEMO31-36 based on MAFFields

	-- Set TRANSACTIONID and CATEGORYID based on MAFFields: *Qualification, STATUS, and TYPE
	
END